# ============================================================
# Blind-Project: Campus Scene Understanding & Telugu Voice Navigation
# requirements.txt — All dependencies for training and runtime
# Python 3.10+  |  Updated: March 2026 (mBLIP migration)
# ============================================================

# ── Core Deep Learning ───────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── Hugging Face (mBLIP model + tokenizer + datasets) ────────
transformers>=4.40.0
accelerate>=0.30.0
datasets>=2.19.0
huggingface-hub>=0.23.0
evaluate>=0.4.1
sentencepiece>=0.1.99
protobuf>=3.20.0

# ── LoRA Fine-tuning — mBLIP campus adapter ──────────────────
# Used in training/train_captioner.py to fine-tune only LoRA layers
peft>=0.10.0

# ── 4-bit Quantization — fits mBLIP in 4 GB VRAM (RTX 3050) ─
# NOTE: On native Windows, bitsandbytes may not install correctly.
# If pip install fails, run in WSL2 or use Google Colab / Kaggle instead.
# If bitsandbytes unavailable, caption_module.py falls back to float16.
bitsandbytes>=0.43.0

# ── Object Detection — YOLO11 ────────────────────────────────
ultralytics>=8.3.0

# ── Computer Vision ──────────────────────────────────────────
opencv-python>=4.9.0.80
Pillow>=10.2.0
numpy>=1.26.0
scipy>=1.12.0

# ── OCR ──────────────────────────────────────────────────────
easyocr>=1.7.1

# ── Text-to-Speech ───────────────────────────────────────────
# edge-tts: Microsoft Neural Telugu voice (requires internet, 800-1500ms latency)
# pyttsx3:  offline English fallback
pyttsx3>=2.90
edge-tts>=6.1.9
pygame>=2.5.0              # Required for edge-tts audio playback

# ── Translation (English danger alerts → Telugu) ─────────────
# mBLIP scene descriptions are already in Telugu.
# deep-translator is used only for English-origin alert strings.
deep-translator>=1.11.4

# ── Caption Evaluation Metrics ───────────────────────────────
nltk>=3.8.1

# ── ONNX + OpenVINO Export (optional — for edge deployment) ──
onnx>=1.15.0
onnxruntime>=1.17.0
openvino>=2024.0.0

# ── Training Utilities ───────────────────────────────────────
tqdm>=4.66.0
tensorboard>=2.15.0
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.2.0

# ── Dataset Download / IO ────────────────────────────────────
requests>=2.31.0
pyarrow>=14.0.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=4.1.0

# ── Misc ─────────────────────────────────────────────────────
colorlog>=6.8.0
psutil>=5.9.8
pynput>=1.7.6              # hotkey listener for Reading Mode toggle
yaml>=0.2.5                # PyYAML alias (ultralytics installs this)
