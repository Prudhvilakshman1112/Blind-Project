# ============================================================
# Blind-Project: Scene Understanding & Voice Navigation
# requirements.txt — All dependencies pinned for reproducibility
# Python 3.10+
# ============================================================

# ── Core Deep Learning ───────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── Hugging Face (BLIP model + tokenizer) ────────────────────
transformers>=4.37.0
accelerate>=0.27.0
datasets>=2.17.0
evaluate>=0.4.1
sentencepiece>=0.1.99
protobuf>=3.20.0

# ── Object Detection — YOLOv11 ───────────────────────────────
ultralytics>=8.1.0

# ── Computer Vision ──────────────────────────────────────────
opencv-python>=4.9.0.80
opencv-contrib-python>=4.9.0.80
Pillow>=10.2.0
numpy>=1.26.0
scipy>=1.12.0

# ── OCR ──────────────────────────────────────────────────────
easyocr>=1.7.1

# ── Text-to-Speech ───────────────────────────────────────────
pyttsx3>=2.90
edge-tts>=6.1.9        # optional higher-quality TTS (caches voices locally)

# ── Caption Evaluation Metrics ───────────────────────────────
nltk>=3.8.1
pycocoevalcap>=1.2      # BLEU / METEOR / CIDEr / ROUGE-L
pycocotools>=2.0.7

# ── ONNX + OpenVINO Export ───────────────────────────────────
onnx>=1.15.0
onnxruntime>=1.17.0
openvino>=2024.0.0

# ── Training Utilities ───────────────────────────────────────
tqdm>=4.66.0
tensorboard>=2.15.0
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.2.0

# ── Dataset Download / IO ────────────────────────────────────
requests>=2.31.0
zipfile36>=0.1.3
gdown>=5.1.0
pyarrow>=14.0.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=4.1.0

# ── Misc ─────────────────────────────────────────────────────
colorlog>=6.8.0
psutil>=5.9.8
pynput>=1.7.6           # hotkey listener for Reading Mode toggle
